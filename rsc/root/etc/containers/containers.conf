# Use crun as the default OCI runtime. Disable default sysctls that write
# to /proc/sys (e.g. net.ipv4.ping_group_range) — those are read-only
# inside a Docker container.
[engine]
runtime = "crun"

[containers]
default_sysctls = []
